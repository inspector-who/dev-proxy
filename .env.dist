COMPOSE_PROJECT_NAME=dev-proxy

TRAEFIK_IMAGE=traefik:v3.6

# Traefik shared network name
TRAEFIK_NETWORK=traefik

# Optional: explicit subnet(s) for the Traefik network to avoid conflicts with VPN/local routes
# Provide a single subnet:
# TRAEFIK_SUBNET=10.123.0.0/16
# Or a list of candidates (space-separated):
# TRAEFIK_SUBNETS="172.30.0.0/16 172.31.0.0/16 10.123.0.0/16"

# Traefik ports (host side). You can override these if 80/443/8081 are busy on your machine.
# Examples:
# TRAEFIK_PORT_HTTP=8080
# TRAEFIK_PORT_HTTPS=8443
# TRAEFIK_PORT_DASHBOARD=8082
TRAEFIK_PORT_HTTP=80
TRAEFIK_PORT_HTTPS=443
TRAEFIK_PORT_DASHBOARD=8081
